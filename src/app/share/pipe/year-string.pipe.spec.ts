import { YearStringPipe } from './year-string.pipe';

describe('YearStringPipe', () => {
  it('create an instance', () => {
    const pipe = new YearStringPipe();
    expect(pipe).toBeTruthy();
  });
});
