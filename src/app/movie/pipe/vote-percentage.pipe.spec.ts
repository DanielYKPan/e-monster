import { VotePercentagePipe } from './vote-percentage.pipe';

describe('VotePercentagePipe', () => {
  it('create an instance', () => {
    const pipe = new VotePercentagePipe();
    expect(pipe).toBeTruthy();
  });
});
